# -----------------------------------------------------------
# PATHS
# -----------------------------------------------------------
out_root: /data/core-rad/data/tucker_predictions
out_id: grid_6

label_root: /data/core-rad/data/tucker/raw/000-tdata/labelsTs
metric_csv_path: results_grid/06.csv
eval_workers: 32

# -----------------------------------------------------------
# TUCKER CONFIG
# -----------------------------------------------------------
fast_model: False
tucker_config:
    tucker_args:
        rank_mode: relative
        rank_factor: 0.2
        rank_min: 8
        decompose: True
        verbose: True
    apply_tucker: True
    inference_bs: 1
    ckpt_path: /tmp/06_tucker_grid.pt
    save_model: True
    load_model: True
