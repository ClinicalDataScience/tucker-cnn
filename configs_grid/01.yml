# -----------------------------------------------------------
# PATHS
# -----------------------------------------------------------
out_root: /data/core-rad/data/tucker_predictions
out_id: grid_1

label_root: /data/core-rad/data/tucker/raw/000-tdata/labelsTs
metric_csv_path: results_grid/01.csv
eval_workers: 32

# -----------------------------------------------------------
# TUCKER CONFIG
# -----------------------------------------------------------
fast_model: False
tucker_config:
    tucker_args:
        rank_mode: relative
        rank_factor: 0.9
        rank_min: 8
        decompose: True
        verbose: True
    apply_tucker: True
    inference_bs: 1
    ckpt_path: /tmp/01_tucker_grid.pt
    save_model: True
    load_model: True
